//var nome = document.getElementById('titulo');
//var descricao = document.getElementById('descricao');

// Selecionar o formulário
//const formulario = document.getElementById('seu-formulario');

// Selecionar o catálogo
//const catalogo = document.querySelector('.grid-container-allpro');

// Adicionar um ouvinte de evento para o envio do formulário
/*formulario.addEventListener('submit', function(event) {
    event.preventDefault();

    // Obter os valores do formulário
    const nomeProjeto = document.getElementById('titulo').value;
    const descricaoProjeto = document.getElementById('descricao').value;

    // Criar um novo elemento div para representar o projeto
    const novoProjeto = document.createElement('div');
    novoProjeto.classList.add('grid-item-a');

    // Preencher o novo projeto com os dados do formulário
    novoProjeto.innerHTML = `
        <h2>${nomeProjeto}</h2>
        <p>${descricaoProjeto}</p>
        <button class="more">Saber mais</button>
    `;

    // Anexar o novo projeto ao catálogo
    catalogo.appendChild(novoProjeto);

    // Limpar o formulário
    formulario.reset();
});
*/